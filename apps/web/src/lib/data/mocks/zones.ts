// Zonas monitorizadas por la agency: vacío hasta que tengamos clientes reales.
// El propio inversor crea sus zonas desde /zonas con NewZoneDialog.

import type { Zone } from '../types';

export const zonesMock: Zone[] = [];
